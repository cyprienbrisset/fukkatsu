<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (night). Uses a platform theme so no AppCompat/Material XML
         library is required; the Compose Material3 theme is applied in-app.
         A proper Compose theme is introduced in a later task. -->
    <style name="Theme.MyPortal" parent="android:Theme.Material.NoActionBar" />
</resources>
